import 'package:flutter/material.dart';

class AlternateMedicinePage extends StatelessWidget {
  const AlternateMedicinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'This is Alternate Medicine Page',
        ),
      ),
    );
  }
}
