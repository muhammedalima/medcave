import 'package:flutter/material.dart';

class AmbulanceUserPage extends StatelessWidget {
  const AmbulanceUserPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'This is Ambulance User Page',
        ),
      ),
    );
  }
}
